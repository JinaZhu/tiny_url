import React from "react";

import { LogoContainer, LogoTag } from "./styled";

const Logo = () => {
  return (
    <LogoContainer>
      <LogoTag>TiniURL</LogoTag>
    </LogoContainer>
  );
};

export default Logo;
